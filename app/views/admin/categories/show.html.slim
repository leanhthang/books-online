= link_to admin_categories_path do
  .btn.btn-outline-secondary.el-space <
.btn.el-space
  = "#{@cat.name}: "
  .badge.badge-light.el-space-r
    = @cat.post_count
.btn.el-space.btn-success
  | Đã Public:
  .badge.badge-light
    = @cat.post_count
.btn.el-space.btn-warning
  | Chưa Public
  .badge.badge-light
    = @cat.post_count
table.table
  thead
    tr
      th Post Name
      th Public
  tbody
    - @posts.each do |post|
      tr
        td
          = link_to post.title, admin_post_show_path(post)
        td.text-success = post.public ? "&#10003;".html_safe : ""
= will_paginate @posts

